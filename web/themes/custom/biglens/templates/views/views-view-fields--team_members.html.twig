<div class="col-sm">
  <div class="team-member">
    <img class="mx-auto rounded-circle" src="{{ fields.field_media_image.content }}" alt="{{ fields.title.content }} - {{ fields.field_role.content }}">
    <h4>{{ fields.title.content }}</h4>
    <p class="text-muted">{{ fields.field_role.content }}</p>
  </div>
</div>
